//import file to test
import { updateUI } from '../src/client/js/updateUI'

//test if the updateUI function is defined
describe("Testing if serverside has express", () => {
			test("Testing the formValidation() function is defined", () => {
				expect(updateUI).toBeDefined();
			})
	});